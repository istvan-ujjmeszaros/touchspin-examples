# TouchSpin React + Vanilla Example

This example demonstrates how to use TouchSpin with React and the vanilla renderer.

## 🚀 Quick Start

```bash
cd examples/react/vanilla
pnpm install
pnpm dev
```

Or from the root:

```bash
pnpm run react-vanilla
```

## 📦 Dependencies

- `@touchspin/react`: React adapter for TouchSpin
- `@touchspin/core`: Core TouchSpin logic
- `@touchspin/renderer-vanilla`: Vanilla CSS renderer

## 🎯 Features Demonstrated

- **Controlled component**: Using `value` and `onChange` props
- **Uncontrolled component**: Using `defaultValue`
- **Props**: `min`, `max`, `step`, `prefix`, `suffix`
- **State management**: `disabled`, `readOnly`
- **Styling**: Vanilla renderer CSS

## 🔧 Configuration

The example uses Vite for fast development and React 18 with hooks for state management.
